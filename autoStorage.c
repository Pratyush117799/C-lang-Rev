#include<stdio.h>
#include<stdlib.h> // for exit()
int main(){
    auto int i=4,j=55; // default initialization to garbage values
   // int k = i + j;
  //  printf("Value of k (i+j) is: %d\n", k);
    printf(" %d %d are the garbage or default values of i and j \n", i, j);
    return 0;
}